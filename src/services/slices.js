import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const globalPropsSlice = createSlice({
  name: "globalProps",
  initialState,
  reducers: {},
});

// export const {  } = globalPropsSlice.actions;

export default globalPropsSlice.reducer;
